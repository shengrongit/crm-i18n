# 远程语言人工覆盖

`sync-remote-i18n.mjs` 生成 vi / km 时：已有译文优先，缺词回落英文。  
本目录补上用户端可见、但历史包从未译过的 key。

| 文件 | 语言 |
|------|------|
| `vi-VN.json` | Tiếng Việt |
| `km-KH.json` | ភាសាខ្មែរ |

只写需要覆盖的 key；同步时覆盖层优先于历史包和英文模板。
